# Copy this file to .env and update values before running the app.
# These values are used by the server and should not be committed.

DATABASE_URL="file:./dev.db"
JWT_SECRET="replace-with-a-strong-random-string"
