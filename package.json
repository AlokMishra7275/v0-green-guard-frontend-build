"scripts": {
  "dev": "next dev",
  "build": "prisma generate && next build",
  "start": "next start",
  "lint": "echo \"No lint configured. Install ESLint or add a config file to enable.\"",
  "prisma": "prisma",
  "db:dev": "prisma migrate dev --name init",
  "db:push": "prisma db push",
  "postinstall": "prisma generate"
}